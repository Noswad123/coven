# Output Directory

Agents should put final runnable artifacts, demos, summaries, and delivery notes here.

For this example, include `demo.py`, an interactive workspace-aware terminal demo agent runnable with:

```bash
python3 output/demo.py
```

The demo should answer questions about what was built, who did what, why decisions were made, which checkpoints were approved, and what QA verified.
